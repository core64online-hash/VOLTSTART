# VOLTSTAR — Чек-лист впровадження

## Phase 0 — Каркас (поточний інкремент)
- [x] Ініціалізація монорепо: pnpm workspaces + Turborepo
- [x] Базові конфіги: TS, ESLint, Prettier, `.editorconfig`, `.gitignore`, `.env.example`
- [x] `apps/web`: Next.js 15 + Tailwind + next-intl (uk/en), стартова сторінка
- [x] `apps/api`: NestJS + Swagger + health-endpoint + скелети модулів bounded contexts
- [x] `packages/`: `config`, `ui`, `types` (zod DTO)
- [x] Prisma: `schema.prisma` зі скелетом ключових моделей + seed
- [x] Selector: сервіс розрахунку потужності + unit-тести
- [x] `docker-compose.yml`: postgres, redis, typesense, mailhog
- [x] `.github/workflows/ci.yml`: install → lint → typecheck → test → build
- [x] `docs/`: ARCHITECTURE.md, ROADMAP.md, CHECKLIST.md, README
- [ ] Локальний запуск перевірено (`pnpm install`, `docker-compose up`, `pnpm dev`)
- [x] Перша Prisma-міграція згенерована в оточенні з БД (`prisma/migrations/*_init`)

## Phase 1 — Каталог + Підбір
- [x] Моделі Product/Category/Brand/ProductSpec/InventoryItem + міграції (`init`)
- [x] API каталогу (список з фільтрами й пагінацією, фасети, товар за slug) + сідінг демо-даних
- [ ] Керування каталогом (створення/редагування товарів) — в адмін-панелі (Phase 6)
- [x] Інтеграція Typesense: індекс через аліас (переіндексація без простою), пошук, стійкий до опечаток, резервний Postgres, синхронізація наявності після оформлення/скасування
- [ ] Фонова переіндексація через BullMQ (зараз: `POST /api/search/reindex`, `pnpm search:reindex`, `TYPESENSE_SYNC_ON_START`)
- [x] Фронт: список каталогу з фасетними фільтрами, сторінка товару
- [ ] SEO каталогу (метадані товару, sitemap, schema.org) — Phase 7
- [x] EquipmentPreset (профілі навантаження типової техніки)
- [x] Форма підбору (UI) → `/api/selector/calculate` → рекомендована потужність
- [ ] Ранжований список товарів під розрахунок у формі підбору (сервіс `rankCandidates` є, UI — ні)
- [x] Тести: розрахунок потужності, пошук (unit + контрактний тест HTTP API Typesense)

## Phase 2 — Акаунти, сегменти, ціни
- [x] Auth (реєстрація/логін), JWT-токени, ролі RBAC (JwtAuthGuard + RolesGuard)
- [x] Organization + типи; верифікація ЄДРПОУ/VAT (контрольна сума ЄДРПОУ)
- [x] Pricing-движок: PriceList/Price за сегментом і валютою (розкладка ПДВ)
- [x] Кабінети профілю за сегментами (B2C/B2B/B2G): login/register/account на web
- [x] Тести доступів (RBAC) і коректності цін за сегментом
- [x] Скидання паролю (email) — одноразові токени (sha256 у БД, 60 хв), зроблено в Phase 4
- [x] Перша Prisma-міграція для Accounts/Pricing в оточенні з БД (увійшла в `init`)

## Phase 3 — Кошик, checkout, платежі
- [x] Cart/CartItem, розрахунок вартості, ПДВ, доставка (Нова пошта / курʼєр / самовивіз, пороги безкоштовної доставки)
- [x] `PaymentProvider`: WayForPay/LiqPay + Stripe + оплата за рахунком (BANK_INVOICE)
- [x] Webhooks + звірка статусів (ідемпотентність за `WebhookEvent`, перевірка підписів, звірка суми)
- [x] Гілки checkout: B2C (карта) vs B2B/B2G (рахунок; лише для верифікованих організацій)
- [x] Резерв складу при оформленні (умовне списання) і відкат при збої провайдера
- [x] Web: кнопка «У кошик», кошик із доставкою та оформленням, повернення з оплати
- [x] Тести: підписи провайдерів, ідемпотентність вебхуків, розрахунки кошика, гілки checkout; e2e на реальній БД
- [ ] Тести оплат у sandbox провайдерів (потрібні тестові облікові дані WayForPay/LiqPay/Stripe)
- [ ] Повернення коштів (refund) через Stripe-вебхуки `charge.refunded` — наступний інкремент

## Phase 4 — Замовлення й документи
- [x] Order/OrderItem, машина станів: ручні переходи менеджера, журнал `OrderStatusEvent`, повернення резерву на склад
- [x] Генерація PDF (рахунок-фактура, видаткова накладна) з кирилицею й сумою прописом — на вимогу
- [ ] Збереження PDF у S3/R2 (зараз рендер на вимогу з незмінного знімка замовлення)
- [x] Сповіщення: email покупцю (оформлення з PDF-рахунком, оплата, відправлення, доставка, скасування), менеджерам — email/Telegram
- [ ] SMS-сповіщення та черга BullMQ для повторних спроб (зараз — best effort, збій лише логується)
- [x] Кабінет: історія замовлень, деталі з історією статусів, завантаження документів; відстеження для гостя (номер + email)
- [x] E2E: повний цикл B2C (картка) і B2B (рахунок) на реальній Postgres + SMTP; UI-перевірки в браузері

## Phase 5 — CRM
- [x] Lead/Contact/Company/Deal/Activity/Task + міграції (адитивна `phase5_crm`)
- [x] Авто-створення лідів із форми підбору (з розрахунком у `payload`) та B2B/B2G-запитів (`/business`); дедуплікація за телефоном/email за 24 год, пастка для ботів
- [x] Pipeline угод (kanban `/manager/crm`), задачі з термінами й простроченням, історія комунікацій (дзвінки, листи, зустрічі, нотатки + системні записи)
- [x] Звʼязок CRM ↔ Orders/Accounts: B2B/B2G-замовлення → угода «Пропозиція» з компанією (ЄДРПОУ/організація) і контактом (акаунт); оплата → «Виграна», скасування/повернення → «Програна»
- [x] Призначення менеджерів: автоматично найменш завантаженому, сповіщення відповідальному (email) і в канали менеджерів; роль — `pnpm --filter @voltstar/api user:role <email> MANAGER`
- [x] Тести бізнес-логіки воронки (переходи лідів і угод, причина програшу, розподіл, нормалізація телефону); e2e API на реальній Postgres + UI-перевірки в браузері
- [ ] Перепризначення ліда/угоди з UI та картка ліда з історією (API вже підтримує `ownerId` і активності ліда)
- [ ] Нагадування про прострочені задачі (черга BullMQ разом зі сповіщеннями Phase 4)

## Phase 6 — Back-office + аналітика
- [x] Адмін-панель `/admin`: каталог (створення/редагування товарів, характеристики, ціни за прайс-листами, залишки), замовлення (пошук, картка, дозволені переходи статусу, звірка оплати за рахунком, документи), клієнти (черга верифікації організацій, ролі користувачів)
- [x] Розмежування: менеджер — дашборд, замовлення, залишки, верифікація; адміністратор — ще каталог і ціни, ролі, журнал. Захист від зміни власної ролі й видалення останнього адміна
- [x] Дашборди: продажі за датою оплати (з поверненнями, за київськими днями), сегменти, топ товарів, воронка лідів і угод (win rate, відкритий pipeline), конверсія підбору «розрахунок → заявка → угода»
- [x] Аудит-лог дій персоналу (`@Audited` на маршрутах: замовлення, оплати, організації, CRM, каталог, ролі, переіндексація); секрети не журналюються; фільтри за обʼєктом і дією
- [ ] Архівування товарів (зняття з вітрини без видалення) і керування брендами/категоріями/прайс-листами з UI
- [ ] Експорт звітів (CSV) і кешування аналітики для великих обсягів

## Phase 7 — Готовність до релізу
- [x] Безпека: ліміти частоти запитів (вхід за IP і email, реєстрація, скидання паролю, заявки, оформлення, пошук замовлення гостем; загальний ліміт), захисні заголовки API (сувора CSP, X-Frame-Options, HSTS у prod) і web (CSP з обмеженням `form-action` платіжними шлюзами), ліміт розміру тіла, Swagger у prod лише явно, `trust proxy`
- [x] Секрети: API не стартує в production з дефолтним/коротким `JWT_SECRET`, http-адресами, `*` у CORS, sandbox-оплатами чи вимкненими лімітами
- [x] CSRF: токен у заголовку `Authorization` (не cookie) — класичний CSRF неможливий; вебхуки перевіряються підписом
- [ ] Перенесення токена з localStorage в httpOnly-cookie + nonce-CSP без 'unsafe-inline' (зменшить наслідки XSS)
- [ ] Спільне сховище лімітів (Redis) для кількох інстансів API
- [ ] Продуктивність: індекси БД, кеш, зображення, Lighthouse
- [ ] SEO (sitemap, robots, schema.org), i18n uk/en, a11y (WCAG AA)
- [ ] Навантажувальні тести
- [x] Бекап і відновлення БД: `scripts/db-backup.sh` (ротація, контрольна сума), `db-restore.sh`, `db-verify-restore.sh` (відновлення в тимчасову БД і звірка таблиць) — перевірено
- [x] Політики: сторінка політики конфіденційності (uk/en, шаблон — перевірити юристу), банер згоди з повторним вибором із футера, посилання на політику у формах
- [x] Права субʼєкта даних: вивантаження своїх даних (JSON) і видалення акаунта (знеособлення профілю, CRM-контакту й заявок; замовлення лишаються для обліку)
- [x] Строки зберігання: `pnpm --filter @voltstar/api data:retention [--dry-run]` — кошики, заявки без угоди, контакти в старих B2C-замовленнях, журнал, токени, вебхуки

## Phase 8 — Випуск у прод
- [ ] Середовища dev/staging/prod + керування секретами
- [ ] CI/CD: автодеплой web + api, міграції БД у пайплайні
- [ ] Домен/DNS/TLS, CDN, моніторинг (Sentry + Prometheus/Grafana), алерти
- [ ] Бекапи БД за розкладом + перевірка відновлення
- [ ] Smoke-тести на prod, плейбук відкату (rollback)
- [ ] Запуск 🚀 + пост-реліз спостереження (2 тижні)
